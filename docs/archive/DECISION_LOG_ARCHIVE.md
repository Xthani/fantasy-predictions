# Decision Log — archive (pre–Phase 1 live API)

Superseded integration decisions kept for history only. **Active log:** [`../DECISION_LOG.md`](../DECISION_LOG.md).

---

## Decision 017 — Incremental API wiring (superseded)

Wire endpoints gradually; mocks for unwired screens. **Superseded:** full Phase 1 API.

## Decision 018 — Remove mocks; cookies auth (superseded)

**Superseded by 019**, then by Phase 1 integration.

## Decision 019 — Strip old backend; mocks for UI (superseded)

Temporary strip of auth/API before `fantasy-predictions-back`. **Superseded:** Phase 1 restored live API.

## Decision 013–016 notes

- **013:** Mock-first onboarding — UI now on live API.
- **015:** `BACKEND_BRIEF.md` workflow — briefs removed from frontend repo; backend owns specs.
