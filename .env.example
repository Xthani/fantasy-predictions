# Скопируй в .env.local при подключении бэкенда (файл в .gitignore).
# Без /api в конце — пути в коде начинаются с /api/...
# Локальный бэкенд (Docker):
VITE_API_BASE_URL=http://localhost:8000
# Альтернатива (тот же API): http://localhost:4000
